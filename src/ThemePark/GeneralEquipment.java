package ThemePark;

public class GeneralEquipment extends Building {
    public GeneralEquipment(boolean inConstruction, int constructionTime, int buildPrice, int location_X, int location_Y, int range) {
        super(inConstruction, constructionTime, buildPrice, location_X, location_Y, range);
    }
}
